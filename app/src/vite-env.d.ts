/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** Build timestamp, injected by vite.config.ts — shown in Settings so a stale PWA is obvious. */
declare const __BUILD__: string
